<table>
 <thread>
  <tr>
   <th>Name</th>
   <th>SKU</th>
   <th>Price</th>
  </tr>
 </thread>

 <tbody>
   <% @products.each do |product| %>
    <tr>
     <td><%= product.name %></td><br />
     <td><%= product.sku %></td><br />
     <td><%= number_to_currency product.price %></td>
   </tr>
  <% end %>
 </tbody>
</table>

</div>

<%= link_to 'New Product', new_product_path %>
