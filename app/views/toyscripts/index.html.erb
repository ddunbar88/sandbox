<table>
 <thread>
  <tr>
   <th>Name</th>
   <th>SKU</th>
   <th>Price</th>
  </tr>
 </thread>

 <tbody>
   <% @products.each do |product| %>
    <tr>
     <td><%= product.name %></td>
     <td><%= product.sku %></td>
     <td><%= number_to_currency product.price %></td>
   </tr>
  <% end %>
 </tbody>
</table>

</div>

<%= link_to 'New Product', new_product_path %>
